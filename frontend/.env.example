# API Base URL - Flask backend always runs on Pi (192.168.1.243)
# Frontend can run on either Pi or Desktop
# Use Pi IP so it works from any device on the network
VITE_API_URL=http://192.168.1.243:5000
